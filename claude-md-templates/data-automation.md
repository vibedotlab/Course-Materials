# [Project Name]

## What This Does
[What data problem does this solve? What does it output?]

## Environment
- Python 3.12
- Install: pip install -r requirements.txt
- Run: python main.py

## Config
- Settings in config.yaml (copy from config.example.yaml)
- API keys in .env — never commit .env
- Output goes to /output/ — gitignored

## Rules
- No external libraries without adding to requirements.txt
- All errors logged to logs/errors.log with timestamp
